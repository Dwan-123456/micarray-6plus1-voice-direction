# L1动态噪声记录与L2后续优化方向（v0.2历史实现）

> 本文仅保留v0.2算法与实验背景，不再是当前实施契约。v0.3已把`cohen_imcra_2003_l1_v1`迁入L1：按Cohen 2003论文流程每20 ms发布7麦80～8000 Hz状态，从500～4000 Hz聚合概率，再由两个概率平均形成40 ms Gate输入；L2已删除Noise Estimation。当前契约以[`ARCHITECTURE_V0.3_TARGET.md`](ARCHITECTURE_V0.3_TARGET.md)为准。

## 当前已实现：只记录，不参与算法

Layer 1在校准后的7通道音频上维护逐麦克风、逐频点的动态噪声功率谱。当前实现采用轻量MCRA式记录器，目的是积累真实环境数据并为后续算法评估提供依据，不改变音频样本，也不参与当前Layer 2的SRP-PHAT、归一化、候选阈值或排序。

每个`DecodedAudio`和`IngestedAudioBlock`可携带只读`NoiseSpectrumRecord`：`psd`为float32 `[7,1025]`，`frequencies_hz`为`[1025]`，并携带48 kHz、2048 FFT、`sequence_id`、`timestamp`和`mcra_record_v1`版本。

访问方式与音频一致：实时消费者从同一个音频块读取`noise_spectrum`；正式录制时，`RecordingStore`沿用音频的`session_id / stream_epoch / start_sample / end_sample`，在同一chunk的`noise_spectrum/*.npz`中保存`start_samples`、`end_samples`、`timestamps`、`frequencies_hz`、`psd [record,7,1025]`、`n_fft`和`estimator`。

记录器在输入重启、sequence跳变或timestamp断裂时重置。疑似强声频点减慢噪声更新，持续背景则逐步进入动态基线。参数来自`config/config.yaml`的`layer1_noise_recording`；正式资产是否写入由`recording.runtime.record_noise_spectrum`控制。

边界必须保持明确：

- 记录器只观察音频，绝不滤波、门控或修改PCM；
- 当前L2不读取`noise_spectrum`；
- 不据此输出声源存在概率，不把记录值称为标注真值；
- 不另建采集线程、时间轴、正式资产根目录或Catalog；
- MCRA式记录器是工程数据记录版本，不宣称完整复现论文IMCRA。

## v0.2历史实现：L2 Noise Estimation与Mean/Std Gate（已删除）

以下内容只记录已被v0.3替换的历史方案，不描述当前代码。当前L2不再包含Noise Estimation、NE后端选择或Mean/Std Gate；IMCRA已迁入L1，L2消费两个与末尾40 ms严格对齐的20 ms概率，取算术平均后按默认0.60的可调门限决定是否执行SRP-PHAT。

旧版Layer 2曾显式经过`Noise Estimation → Sound / Energy Gate → SRP-PHAT`，其历史编排状态为：

- `BYPASSED`：仅用于显式诊断旁路配置，Noise Estimation正常运行且SRP照常运行；
- `BLOCKED`：预热、背景噪声或无效输入使Gate关闭，SRP不运行且不生成空间谱；
- `PROCESSED`：Gate放行后执行SRP。

Noise Estimation保留默认后端`imcra_v1`，并新增可切换的`dual_stat_v1`。两者都直接读取原有48 kHz `DecisionWindow [15360,7]`，首窗处理其中16个20 ms帧，后续只消费新增960 samples；都以2048点periodic Hann STFT计算7通道功率谱并逐频点取中位数，频带统一为500～4000 Hz，不读取L1的`NoiseSpectrumRecord`，也不修改PCM。

`imcra_v1`保持原行为：维护局部最小统计、后验声源存在概率、Noise PSD及10秒指数噪声level均值/方差。`dual_stat_v1`是独立NumPy实现，借鉴固定先验SPP和在线低分位噪声跟踪思路，但没有移植WebRTC或SpeexDSP源码。它以最后40 ms作为Gate观测，使用20%声音先验的SPP，并融合背景level偏差、带内SNR、逐bin SPP和能量加权频谱形状差异，将每帧分成`BACKGROUND / FOREGROUND / UNCERTAIN / INVALID`；`BACKGROUND`完整更新Gate消费的瞬时背景统计，`UNCERTAIN`按分数做截尾软更新，`FOREGROUND`冻结背景统计并维护独立事件统计。背景标准差下限调整为1.25 dB，以提高约50 cm弱声召回。连续稳定、低波动的非背景事件经过10秒确认后晋升为背景，降低持续声音被过早吸收的风险。两后端共享基础输出`ready / noise_mean_db / noise_std_db / noise_level_db / signal_level_db / snr_db / noise_psd[1025]`，新后端另输出分类和启发式前景分数诊断；该分数不是校准概率。

Sound/Energy Gate默认后端仍为`mean_std_v1`，代码和公式均未改变。Noise Estimation预热完成且所需数值有效后，每个`DecisionWindow`独立计算`threshold_db = noise_mean_db + 2 * noise_std_db`；仅当`signal_level_db > threshold_db`时开启，等于或低于门限时关闭。`imcra_v1`继续提供原20 ms信号与平滑Noise PSD level统计；`dual_stat_v1`提供与SRP同为40 ms的信号level，以及由背景完整更新、不确定软更新、前景冻结形成的背景统计。当前Gate不以`snr_db`作判据，不使用8/4 dB SNR滞回，也不包含Gate级attack/release状态。Noise Estimator在Gate关闭时仍逐hop运行；Gate只决定是否让同一个原始`DecisionWindow`进入SRP，不修改PCM。阻断后不得沿用旧方向，也不得伪造全零360°响应。

Development Test UI的Noise Estimator按钮可在上述两种后端间切换并持久化选择。运行时只记录请求值；唯一processing线程在下一完整窗口边界构造新后端并重置NE和Gate。因此切换后的首批结果必定重新进入`WARMING_UP/BLOCKED`，不会把旧后端的PSD、分类或均值/方差带入新后端。

算法依据与边界：固定先验SPP参考[Gerkmann与Hendriks 2012](https://doi.org/10.1109/TASL.2011.2180896)，minimum-controlled路线参考[Cohen IMCRA](https://doi.org/10.1109/TSA.2003.811544)，在线低分位工程思路参考[WebRTC Noise Suppression](https://webrtc.googlesource.com/src/+/refs/heads/main/modules/audio_processing/ns/)。仅凭当前能量/频谱统计，无法从第1帧可靠区分“永久存在的稳定目标声”和“永久存在的设备底噪”；当前实现选择把长期平稳成分归入背景。若验收需要保留前者，必须进一步引入阵列空间相干、已标定噪声先验或语义分类。

## 后续可能优化：当前全部未接入

以下项目仅作为后续实验路线。启用任何一项前都必须新增离线基准、版本号、配置、回退方式和实机验收，不能在当前L2中静默改变行为。

### 1. 可靠性加权PHAT（RWPHAT）

由噪声PSD计算每通道、每频点先验SNR及Wiener型可靠度：

\[
g_m(k,t)=\frac{\xi_m(k,t)}{1+\xi_m(k,t)}
\]

麦克风对权重可进一步结合跨麦相干性和通道健康度：

\[
w_{ij}(k,t)=\sqrt{g_i(k,t)g_j(k,t)}\,C_{ij}(k,t)Q_{ij}(t)
\]

目标是压低被底噪、混响或坏通道支配的频点。可评估diffuseness/CDR掩码，但不得对7个通道分别执行会改变相位关系的非线性降噪后再定位。

### 2. 有效证据总量

加权结果不能只除以`sum(w)`，否则少数随机频点可能再次被放大。候选至少应记录并验证：

\[
N_{eff}=\frac{(\sum w)^2}{\sum w^2}
\]

以及有效频点比例、总权重、健康麦克风数和几何分散的麦克风对支持率。通道健康检查应限定在定位频带，而不是只看全频RMS。

### 3. 每个候选的绝对空间证据

每个候选方向分别计算噪声白化后的绝对相干响应、麦克风对一致率、有效频点比例、带限相对SNR、局部prominence、第一/第二峰差、峰宽和曲率。21个麦克风对采用截尾或鲁棒汇总，避免单个异常通道污染其关联的6个pair。

不能先用一个全局“本帧有声”开关，再让全部相对Norm峰通过；多候选场景必须逐候选检验，否则真声源的旁瓣仍会成为假候选。

### 4. CFAR声源存在门限

从多场景无源录音估计`H0: 只有环境背景`。门限针对每帧`max_theta`的完整360°搜索分布标定，而不是针对单角度或当前Sigmoid值。目标应写成误报次数/分钟、最低输入SNR下的检出率和最大起检延迟。在线更新必须在疑似活动时冻结或减慢，防止持续弱源被吸收进背景。

### 5. 时间积累与滞回

强瞬态可由高绝对证据单帧开启；弱而持续的声源通过K/N帧且角度落在圆周邻域内后开启。建立门限高于维持门限，退出要求连续多帧证据不足。该状态只用于候选确认和去抖，不等同于人物Tracking，也不产生`source_id`。

### 6. 验证和语义边界

评估至少覆盖安静、空调、风扇、电脑、室外噪声，不同方向、距离、响度、混响和坏通道；报告误报/分钟、事件召回、起检/释放延迟以及检出条件下的DOA P50/P95。

无法同时保证任意微弱声源全部检出且永不误报。定向风扇、电视等从阵列物理特征看就是声源；若目标只限人声，应在后级方向增强后使用VAD或L4分类，不能让L2的Normalized分数承担语义分类。

参考方向：Cohen的MCRA/IMCRA噪声估计、Valin等人的RWPHAT、SRP-CFAR，以及ODAS中定位候选与后级时间状态分离的架构。ODAS的`ssl.probMin`不是声源存在阈值，不能直接照搬。

## 多声源定位优化：当前全部未接入

实际双声源或多声源测试中，当前单次全频SRP图经常只形成一个主峰，并在不同真实声源方向之间逐帧切换。主要原因是所有频点和21个麦克风对一次性汇总为一条360°曲线：不同声源在同一窗口内竞争，较强声源、相干交叉项和旁瓣会压制第二声源；40 ms内不同音素和频带的优势变化又造成主峰闪动。

以下四项只记录为后续优化方向，当前不修改L2算法、配置、DTO或UI行为。

### A. 时频子带SRP投票与圆周聚类

不先把500～4000 Hz全部合并成一张空间图，而是按子带或时频块分别计算方向响应。每个可靠块输出局部方向峰，再按SNR、相干性、有效频点数和麦克风对支持率进行圆周投票或聚类。

该方法利用人声的时频稀疏性：同一时频块通常由某一个声源占优，因此全频汇总中被合并的多个方向可能在子带投票后形成多个方向簇。聚类必须按圆周距离处理0°/359°边界，并设置最小独立频带支持数，避免单个窄带干扰形成假声源。

### B. 多方向时间证据槽

为最多若干个方向维护相互独立的证据槽。新峰与已有槽在角度邻域内时累积证据；短时缺失只衰减，不立即删除；新方向需连续确认，释放使用更低门限和更长缺失条件。

该机制用于保存“同一时间存在但逐帧交替占优”的多个方向，降低A/B方向闪动。它是候选级时间稳定器，不进行人物身份关联，不产生`source_id`，也不应被描述为Speaker Tracking。

### C. 迭代式多峰搜索（重点方向）

#### 当前实验实现（`iterative_rank1_projection_v1`）

该方向现已作为可选实验路径接入Layer 2，默认关闭，Test UI可在右侧候选表上方切换。关闭时严格运行原单次SRP-PHAT路径；开启后从下一个完整DecisionWindow起生效，最多搜索两个方向，不触碰Layer 3实现。

当前实现保留第0轮未抑制的360°空间图作为蓝环。找到第一个可信方向后，在7通道RFFT观测上构造该方向的远场导向矢量，按频点估计rank-1方向成分，并用阵列匹配度进行软投影消除；随后重新计算21对互谱、PHAT/GCC和完整360°扫描。残余互谱带有相对原始互谱幅度的有效度保护，避免完全PHAT将微小数值残渣重新放大。

后轮归一化固定使用第0轮MAD尺度，不重新估计鲁棒尺度；并检查有效频点、麦克风对支持、残余/首峰比例、圆周NMS和剩余证据。任一条件不足时允许只输出一个方向。每个窗口记录实际模式、算法版本、配置revision、轮数和停止/回退原因；迭代异常时该窗口回退单次搜索，但不自动关闭用户开关。

该实现仍属于实验优化：相近方向、强弱差较大、强混响及两个扬声器播放完全同步信号时不保证分离。参数必须通过真实双源录音继续标定。

迭代式搜索不是在最终360°曲线上找到第一峰后，把附近15°清零再找第二峰。简单角度清零只隐藏峰顶，第一声源仍然保留在互谱、GCC和整个空间旁瓣中，容易让第二次搜索得到第一声源的旁瓣或镜像。

推荐流程：

1. 从原始多通道STFT或PHAT互谱计算第一张空间响应，得到当前最强方向`theta_1`；
2. 检查`theta_1`的绝对证据、有效频点数、麦克风对一致率及局部峰形；证据不足则停止，不为了填满Top-K强制产生候选；
3. 找出支持`theta_1`的时频点、麦克风对或低维空间成分，建立第一声源的解释模型；
4. 在原始观测域中将已解释贡献投影掉、降权或从后续匹配中排除，生成残余观测；
5. 对残余观测重新执行完整360°扫描，得到`theta_2`；
6. 对`theta_2`重新执行独立证据检验，并确认它与已接受方向具有足够角度和传播时延差异；
7. 重复“解释→残余→重扫”，直到残余证据不足、达到最大源数或新增候选与已有源不可区分。

可评估的三种实现层级：

- 时频占用抑制：第一方向只占用最支持它的可靠时频点，剩余频点重新扫描。实现相对简单，适合非相干人声，但强源与弱源共享频带时可能过度抑制；
- GCC/麦克风对贡献抑制：根据第一方向理论TDOA，对相符的pair-frequency贡献降权，再重算SRP。必须保留足够几何分散的麦克风对，防止残余图失真；
- 子空间投影：参考多源SVD-PHAT思想，把已找到方向对应的低维成分投影到正交补空间，再搜索下一个方向。对重叠频带和强弱声源更有潜力，但实现、数值稳定性和标定成本最高。

每轮接受候选至少需要：

- 残余绝对空间证据通过独立门限或CFAR；
- 有效频点总量及`N_eff`充分；
- 足够多且几何分散的麦克风对支持该方向；
- 与已接受方向之间不仅角度分离，而且TDOA模式可区分；
- 从观测中消除该候选后，残余解释误差确实下降；
- 候选不是上一声源的已知旁瓣、镜像或混响路径。

停止条件必须优先于`max_candidates`。`max_candidates`只是上限，不是每帧必须填满的数量。建议保留“无第二个可信源”的输出能力，避免迭代搜索把噪声和混响依次解释成多个声源。

关键风险：

- 第一轮方向错误会污染所有后续残余，因此第一候选门限应高于普通局部峰门限；
- 过度消除会连同角度接近或频带重叠的弱声源一起删除；
- 消除不足会重复发现同一个强源或其旁瓣；
- 两个扬声器播放完全相同的同步信号时属于相干声源，普通时频抑制可能失效，应优先用独立信号测试，并评估子空间方法；
- 简单幅度谱相减不能可靠消除源，且可能破坏跨通道相位，必须在保持阵列相位关系的互谱、TDOA贡献或子空间中处理。

建议实验顺序：先实现离线诊断版，仅允许最多两轮搜索；用两个互不相关的语音源，从90°以上间隔和接近响度开始；再逐步测试较小角距、10～20 dB强弱差、混响及部分同频重叠。记录每轮空间图、被抑制的时频比例、残余证据、候选角度和停止原因，确认第二峰来自独立证据而非第一峰旁瓣后，才考虑实时接入。

验收指标应按源数分别统计：零源误报、单源被错误拆成多源的概率、双源“两者都检出/只检出一个/均未检出”的比例、角距与强弱比下的召回率、每个候选DOA误差、重复候选率以及每轮计算耗时。测试信号必须同时包含独立源和相干源，二者结果不得混为一类。

### D. 逐候选绝对验证与后级确认

无论候选来自时频聚类还是迭代重扫，每个方向都必须单独验证绝对相干响应、有效频带、麦克风对共识、局部峰形和跨帧证据。不能因为本帧已经确认一个真实声源，就让其余相对Norm峰自动通过。

后续可对每个候选分别执行L3方向增强，再由L4声类判断确认是否为人声。这一步负责减少多峰能力提升后新增的旁瓣和混响假候选，但不能替代L2本身的多源空间证据。

### 推荐组合顺序

建议先离线实施“时频子带投票”，确认原始观测中确实存在多个方向证据；随后以最多两轮的迭代式多峰搜索为主体，逐候选绝对验证作为每轮接受和停止条件，最后加入多方向时间证据槽消除跨帧闪动。四项组合关系为：

```text
时频子带证据
    → 第一方向搜索
    → 原始观测域解释/抑制
    → 残余重扫第二方向（迭代）
    → 每个候选独立验证
    → 多方向时间证据槽稳定输出
```
